<!--
  ClickHouse system-log control for Deckgauge staging.
  Loaded from /etc/clickhouse-server/config.d/ — merges into the main config.

  WHY THIS EXISTS
  ───────────────
  On 2026-06-19 the ClickHouse container sat at 200%+ CPU while serving
  only ~6 user queries/min (well under 1s of real query time). The cost
  was entirely ClickHouse's OWN telemetry: the built-in *_log tables
  flush a fresh batch every second and then merge it against millions of
  accumulated rows — none of which had a TTL. asynchronous_metric_log
  alone had grown to 103,216,107 rows. Nothing in Deckgauge reads these
  tables (there is no Grafana / observability stack on this box), so the
  work was pure waste.

  FIX
  ───
  - Disable the high-frequency / deep-profiling logs outright.
  - Keep query_log and part_log (genuinely useful for debugging) but cap
    them with a 3-day TTL so they can never balloon again.

  Re-enable any of these by deleting the corresponding block and
  redeploying, if you ever need the profiling data.
-->
<clickhouse>

  <!--
    Per-second system-metric snapshots. asynchronous_metric_log writes
    ~hundreds of rows every second; metric_log writes one wide row every
    second. Together they were the bulk of the constant flush+merge load.
    Disabled via the config-merge remove="1" directive (removes the node
    that the stock config.xml defines).
  -->
  <asynchronous_metric_log remove="1"/>
  <metric_log remove="1"/>

  <!--
    Query-profiler stack samples (trace_log) and per-processor profiling
    (processors_profile_log). Deep-debug telemetry only — not used here.
  -->
  <trace_log remove="1"/>
  <processors_profile_log remove="1"/>

  <!--
    Materialized-view execution log. Deckgauge's MVs run fine without it;
    nothing reads this table.
  -->
  <query_views_log remove="1"/>

  <!--
    The server's own text log, mirrored into a table. MEASURED on the shared test
    ClickHouse at 472 MiB across 11.7 MILLION rows — the largest system table
    there by a factor of four, against 65 KiB of actual `cockpit` data, and the
    only one left with nothing bounding it after the cache caps landed.
    Nothing reads it — repo-wide, `system.text_log` appears only in comments and
    in STATE.md prose — and the table is a queryable MIRROR of a log that also
    exists as a file.

    WHAT THIS GIVES UP, stated because the first version of this comment got it
    backwards. The server's own log is at
    `/var/log/clickhouse-server/clickhouse-server.log` inside the container
    (plus `…err.log`), read with `docker exec … tail`, NOT with `docker logs` —
    measured on staging, `docker logs` yields **8 lines** of entrypoint output
    while the file had **1,019,243**. And `/var/log/clickhouse-server` is not a
    volume (`docker inspect` shows only `/var/lib/clickhouse`), so the file log
    is LOST on a container recreate, where `system.text_log` lived in the data
    volume and survived one. That is the real trade, and it is still worth
    making: 472 MiB across 11.7M rows, against 65 KiB of actual `cockpit` data,
    on a server capped at 2.5 GiB.
  -->
  <text_log remove="1"/>

  <!--
    Per-query metric snapshots — the same high-frequency family as `metric_log`
    above, and disabled for the same reason.

    `error_log` is deliberately NOT in this list. It is small (72 KiB observed)
    and it is the one system log whose contents someone would actually want
    during an incident, so the omission is a decision rather than a gap. This
    enumeration is otherwise complete as of ClickHouse 25.8; a later version
    adding a new `*_log` will not appear here until someone looks.
  -->
  <query_metric_log remove="1"/>

  <!-- Keep query_log (useful for debugging slow/failed queries) but bound it. -->
  <query_log>
    <database>system</database>
    <table>query_log</table>
    <ttl>event_date + INTERVAL 3 DAY DELETE</ttl>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
  </query_log>

  <!-- Keep part_log (merge/insert debugging) but bound it. -->
  <part_log>
    <database>system</database>
    <table>part_log</table>
    <ttl>event_date + INTERVAL 3 DAY DELETE</ttl>
    <flush_interval_milliseconds>7500</flush_interval_milliseconds>
  </part_log>

</clickhouse>
